---
const { posts = [] } = Astro.props;
---
<aside class="recent-posts">
  <h3 class="h5">Recent Posts</h3>
  <ul class="list-unstyled">
    {posts.map(p => (
      <li class="mb-1"><a href={`/blog/${p.slug}`}>{p.data.title}</a></li>
    ))}
  </ul>
</aside>
